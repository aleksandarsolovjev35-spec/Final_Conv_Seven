# Зависимости для запуска тестов и проверок кода.
# Production-зависимости — в requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0,<9
pytest-cov>=5.0,<7

# Требуется starlette.testclient (FastAPI TestClient) в tests/test_ui_server.py.
# Без него сбор тестов падает ещё до первого теста.
httpx>=0.27,<0.29

# Проверки стиля и неиспользуемого кода (настройки — в setup.cfg).
pycodestyle>=2.11,<3
pyflakes>=3.2,<4
